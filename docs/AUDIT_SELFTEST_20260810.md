# 飞越信用·点滴进步 — 小程序提审自测报告

> 审计日期：2026-08-10 ｜ 审计范围：生产环境前后端全量 + 多角色模拟（ADMIN/TEACHER/REP/STUDENT）+ 隐蔽违规点专项扫描
> 审计结论：**已修复 P0 提审阻断 2 项、P1 安全越权 4 项；部署包已重打并通过合规校验；残留 1 项战略级风险需用户决策。**

---

## 一、本次审计发现的全部问题（按严重程度）

### 🔴 P0 — 提审阻断（已全部修复，提交 `59086b3`）

| # | 问题 | 整改前（违规） | 整改后（合规） | 影响 |
|---|---|---|---|---|
| P0-1 | 22 个中小学/中考类 HTML 被公网无鉴权托管 | `server/study-content/` 下含「初三语文·中考总复习」「初三数学·中考重点」等 22 个文件，由 `index.js` 的 `app.use('/study', express.static(...))` 公开托管 | 删除 22 个文件 + 移除 `/study` 公开路由 | 微信对小程序**可达的 H5/外链同样审核**，无内容审核的中考资料 = 必拒 |
| P0-2 | 资源库 copy-link 404 | 种子资料 `url` 指向 `/api/mp/resources/file/<id>`，但该路由不存在 → 用户复制链接打开即 404 | 新增沙箱化文件路由 `GET /api/mp/resources/file/:filename`：仅服务 `study-content/` 内白名单 `.json`，严格 `FILE_NAME_RE` 防路径穿越 | 资料库"复制链接"功能恢复可用，且无 K12 暴露 |

### 🟠 P1 — 安全越权（已全部修复）

| # | 问题 | 整改前 | 整改后 |
|---|---|---|---|
| P1-1 | 角色提权 | 任意 ADMIN 可把任意账号角色改为 ADMIN（横向提权） | `users.js /:id/role` 限制**仅 `superadmin`** 可变更角色 / 授予 ADMIN |
| P1-2 | 学生自产积分 | 学生自报完成状态为 `DONE_ONTIME` 直接计入学分（自产自销） | 学生自报统一记为 `SUBMITTED`（0 分，待老师/课代表确认） |
| P1-3 | 弱口令 `123456` | 重置/创建学生硬编码 `123456`，前端弹窗显示"已重置为 123456" | 改随机 10 位临时密码（`genTempPwd`）+ `must_change_pwd=1`；前端展示真实临时密码 |
| P1-4 | （同 P0-2 的资料 404） | — | 见上 |

### ⚪ P2 — 已排查无问题项（专项扫描结论）

| 扫描项 | 结论 |
|---|---|
| 外链 / web-view 跳第三方域名 | ✅ 未发现（统一走 `wx.callContainer` 内部通道） |
| 诱导分享 / 下载 APP / 关注公众号 | ✅ 未发现 |
| K12 学科字眼（数据库层） | ✅ `normalizeK12ToNeutral()` 已迁移为中性兴趣标签 |
| 虚拟支付 / 电商 | ✅ 未发现 `requestPayment` |
| 绝对化 / 医疗功效 / 党政军 | ✅ 未发现 |
| 前端硬编码密钥 | ✅ `config/env.js` 用环境变量，无明文 AppSecret |
| 死链 / hidden 违规文案 | ✅ 已清理 `preview/` 调试产物 |
| `requiredPrivateInfos` 字段 | ✅ 已清空为 `[]`（此前误填非位置类 API，已纠正） |

---

## 二、提审前自查清单（照打钩，不漏项）

### 阶段 ① 隐私指引审核（公众平台，需 2~5h）
- [ ] 左下角头像 → 账号设置 → 服务内容声明 → 用户隐私保护指引 → 选「本小程序处理了用户信息」→ 勾 **相册 / 摄像头 / 剪贴板 / 文件** 4 项并填用途说明
- [ ] 提交后等待审核状态变「已通过」

### 阶段 ② 上传小程序代码（微信开发者工具）
- [ ] 顶部「上传」→ 版本号 `1.0.1` → 备注「生产环境整改-隐私合规+功能优化」
- [ ] 确认上传成功弹窗（此轮代码已含 `requiredPrivateInfos:[]` 修复）

### 阶段 ③ 提交审核（mp.weixin.qq.com → 管理 → 版本管理）
- [ ] ⚠️ **底部务必勾选「采集用户隐私」**（否则即使指引通过，线上隐私接口仍调不了）
- [ ] 类目选「工具 > 信息管理」或个人主体允许的非教育类目

### 阶段 ④ 发布 + 真机验证
- [ ] 审核通过 → 发布上线
- [ ] 真机验证：发帖选图（相册/拍照）、复制分享链接、附件选 PDF/音频 4 个隐私接口均正常弹授权

### 阶段 ⑤ 服务端部署（与 ②③ 并行）
- [ ] 云托管控制台 → 版本管理 → 新建版本 → 代码包上传 → 选 `dist/server-cloudrun.zip`
- [ ] 部署后访问 `/api/health` 返回 `{"ok":true}`

---

## 三、部署包状态

| 项 | 值 |
|---|---|
| 路径 | `C:\Users\20242\WorkBuddy\feiyue-credit\dist\server-cloudrun.zip` |
| 文件数 | 399 |
| 体积 | ~28.9 MB |
| K12 `.html` | 0 ✅ |
| `/study` 公开路由 | 已移除 ✅ |
| 沙箱文件路由 | 已加入 ✅ |
| 硬编码 `123456` | 0 处 ✅ |
| 含 `.env` | 否 ✅ |
| 函数调用烟雾测试 | 通过 ✅ |

---

## 四、⚠️ 战略级风险（需用户决策，未擅自改动）

**产品形态与个人主体资质存在根本冲突**：应用当前仍是「班级管理 + 学生/教师/课代表角色 + 作业提交 + 学分体系 + 教师后台」结构，属于 K12 校外教学管理语义。个人主体严格不合规。

- 已做的「兴趣标签替换」只解决了**学科字眼**层面的问题；
- **角色、作业、学号、课程资料、教师后台**这些结构体尚未剥离；
- 即便字眼全清，仍可能因「发布作业 / 学生管理 / 学号体系」被审核驳回。

**可选路径（需你拍板）：**
- **A. 维持现状推进**：接受风险，先按上述流程提审，被驳回再针对性改；
- **B. 产品重构**：剥离学生/教师/课代表角色、作业、班级、学号、课程资料、教师后台，改造成「纯个人成长 / 兴趣打卡记录」工具（最小可行方案可另出）。

---

## 五、下一步操作顺序（建议）

1. 走完上面「阶段 ①~⑤」；
2. 关于战略风险，确认走 A 还是 B；
3. （强烈建议）公众平台「重置 AppSecret」并同步更新本地 `server/.env`，彻底脱离 git 历史中的旧密钥残留。

> 配套文档：`docs/SUBMISSION_CHECKLIST_20260810.md`（更细的分步清单）、`docs/PROD_QA_REPORT_20260810.md`（首轮 QA 报告）。

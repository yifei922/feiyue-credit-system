# 2026-09-07 工作日志

## 7 项综合改造上线（自动推送 + 修复上线事故）

### 推送
- GitHub 恢复可达（api 200 / :22 OPEN / :443 OPEN），本地提交 `81d38e1`（7 项改造）+ `a53d53f`（db 修复）已推送 master → Render 自动重建。
- 推送前发现**本地 .git 对象严重损坏**（之前手工改 packed-refs/松散 ref 遗留）：`git fetch` 报 `bad object / bad parents / inflate: data stream error`，packed-refs 里还有 `refs/heads/master?` 坏行（带问号）。
- **解法（可复用）**：源码文件无损 → 干净重克隆到 `feiyue-credit-fresh` → 覆盖 src → `git config core.autocrlf false` → 只 add 真实改动文件 → `git checkout -- .` 清掉 66 个 CRLF/LF 噪声文件 → 提交推送 → 最后把新 `.git` 复制回原目录（旧的重命名为 `.git.corrupt.bak/` 并加 .gitignore）→ `git fetch --unshallow` 补全历史。现仓库 99 条历史、tracking 正常。

### 上线后发现的致命 bug（已修）
1. **学生名单永远导不进去**：`student.gender` 列在 `migrate()` 靠后的 6b 步才 ALTER，但 `importClassRoster()` 在最前面（第 522 行）就调用并 INSERT gender → 首次运行 `Unknown column 'gender'` 中断整个 migrate。
   → 修复：gender 列补齐提到 `migrate()` 第 0-pre 步（早于 clearAndRebuild/importClassRoster）。
2. **每次重启都清库（灾难级）**：`seed()` 里 `INSERT INTO badge` 无 IGNORE，而 `clearAndRebuild()` 不清 badge 表 → 徽章 code 唯一键冲突 → seed 抛错 → `clear_rebuild_2026_09_05` 标记写不进去 → 每次重启重复 `DELETE FROM sys_user`。
   → 修复：徽章改 `INSERT IGNORE`；clearAndRebuild 用 try/catch 吞掉 seed 异常并**保证写入迁移标记**；importClassRoster 也包 try/catch（失败不阻断 migrate，下次重启自动重试）。

### 生产核验（2026-09-07 00:46）
- `/api/health` 200；迁移标记三条齐全：`clear_rebuild_2026_09_05` / `import_class_roster_2026_09_05` / `k12_to_neutral_v1`
- sys_user 56 = 2 ADMIN（斐越科技、杨进杰老师）+ 54 STUDENT；student 54 人（男 30 / 女 24）
- 登录实测：杨进杰老师/17344694075 ✓、斐越科技/18603792929 ✓、杨易轩/810810 ✓（mustChangePwd=true）、雷鑫怡/810810 ✓
- 前端资源已上新：CSS 含 `backdrop-filter`（dialog.css 重影修复），JS 含「课代表工作台」（RepPortal）

### 待办
- 图1 第 5 行被高亮遮挡的名字，暂用「李茗」占位，需杨老师核对修正
- 库内暂无 REP（课代表）账号，需管理员在人员管理里把学生设为课代表

---

## 教师 Tab 按钮缺失 + 嵌套弹窗重影 + 课代表双身份（推送 `38bddf8`）

### 用户反馈
- 图1 教师 Tab 找不到「新增老师/重置密码/删除」按钮
- 图2 嵌套弹窗（管理任务里新建任务）有重影 + 视觉不美观
- 要求验证课代表双身份：设为课代表后该同学自动获得 REP 权限，同时保留学生身份（可提交作业）

### 根因 & 修复
1. **MainLayout.vue 引用 `Medal` 图标但未导入** → 整个布局渲染中断 → 教师 Tab 及所有新增按钮不可见
   - 修：补齐 `Medal` 到 icons-vue 导入列表
2. **TaskList 嵌套弹窗未加 `append-to-body`** → 内层遮罩太透明 → 底层内容透出像重影
   - 修：`formVisible` 与 `compVisible` 弹窗加 `append-to-body`
   - 同步把 `styles/dialog.css` 全面重做：Stripe/Linear 风圆角 + 阴影 + 滑入动画 + 不透明遮罩 + backdrop-filter
3. **创建账号 500 错误**（推送前未发现）：`ReferenceError: isSuper is not defined`
   - 根因：任务 7 改造时把 `isSuper` 改名 `isAdminUser`，但 POST /users 第 124 行遗漏
   - 修：改回 `isAdminUser`

### 课代表双身份机制（任务 6）
- 后端 `POST /api/users/:id/role` 放开给 `ADMIN/TEACHER`：
  - ADMIN（斐越科技、杨进杰老师）可改任何角色
  - 普通 TEACHER 仅做 STUDENT↔REP 互转（防越权升 ADMIN/TEACHER）
- 后端 `GET /api/students` 列表 JOIN sys_user 返回 `role` 字段
- 前端 `StudentManage.vue` 学生行加「设为课代表 / ✓ 课代表(取消)」按钮（`toggleRep`），学生 Tab 加身份列（绿标课代表 / 灰标学生）
- 课代表候选范围扩大为「教师 + 已设为 REP 的同学」（双身份）
- 提示文案改为「在学生 Tab 直接设课代表，保留学生身份」
- 提示：rbac.js 第 82 行已实现 REP→effectiveRoles=['REP','STUDENT']，login token 已含此字段；StudentPortal.vue 已用 effectiveRoles 判断可提交作业

### 端到端验证（本地后端连生产库 8090）
- 杨进杰老师登录 → 设杨易轩为课代表 200 → 杨易轩重新登录 effectiveRoles=['REP','STUDENT'], studentId=69 保留
- 普通教师尝试改学生为 ADMIN → 403 拒绝「教师只能设置/取消课代表（STUDENT↔REP）」
- 取消杨易轩课代表 → 200 → role 回 STUDENT
- 创建账号 200 OK（修 isSuper 后）
- 渲染端 curl 验证：教师 Tab 学生列表带 role 字段、setUserRole(REP) 200、effectiveRoles 双身份正确、studentId 保留

### GitHub 推送
- `ab072b6..38bddf8 master -> master`（38bddf8）
- Render 自动重建后 `/api/health` 200，生产实测全部通过

---

## 杨进杰「进不去」复盘（用户反馈 → 经验归档）

### 用户反馈
> 杨进杰账号进不去，输入账号密码没反应，进不去。以此类推，都查查是不是还有类似问题。斐越科技可以登录。

### 真因追查
1. **生产服务当时可用**：`curl /api/health` 200 OK。
2. **dist 是最新的**：`MainLayout-B32aJDfl.js` 含 `Medal`；`StudentManage-CHTxPzlp.js` 含 `toggleRep`；`TaskList-BypNViOr.js` 含 `appendToBody`——38bddf8 所有修复均已上线。
3. **API 直测杨进杰老师 + 17344694075 → 200 OK**，跳转 `/#/dashboard`，提示「登录成功」。
4. **结论**：用户本地登录问题被复现不到，更可能是浏览器自动补全/输入时多按或少按字符；与 dist 陈旧或后端 bug 无关。

### 数据库核对（用 `node _shots/db_check_yjj.cjs` 直连生产 MySQL）
- 列：`id, username, password, name, role, class_id, student_id, openid, avatar, must_change_pwd, create_time`
- 管理员账户：
  - `id=117, username='斐越科技', role=ADMIN`
  - `id=118, username='杨进杰老师', role=ADMIN`
- **`username='杨进杰'`（不带"老师"）的记录不存在**——`routes/auth.js:18` 是 `WHERE username=?` 严格匹配，所以"杨进杰"+此密码会被服务端返回 401。

### 用户最终确认（2026-09-07 12:52）
> 杨进杰，杨进杰老师均可登陆，密码一样

**事实归档**：用户实际前端输入的两个名**都可能进入 dashboard**（亲口确认）；DB 仅有 `杨进杰老师`。最可能的解释是浏览器自动补全把"杨进杰"补全为"杨进杰老师"，加上密码相同，用户感知为"两个都能登录"。

### 已固化的长期记忆
- `MEMORY.md` 新增段落「登录凭据与账户别名」：列清两个管理员账户、学生初始密码，标注「杨进杰」严格来说不存在，未来若用户真的报 401，再考虑后端兼容。
- `routes/auth.js` 未改动（最小侵入原则）。

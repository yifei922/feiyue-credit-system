# 2026-09-07 工作日志

## 7 项综合改造上线（自动推送 + 修复上线事故）

### 推送
- GitHub 恢复可达（api 200 / :22 OPEN / :443 OPEN），本地提交 `81d38e1`（7 项改造）+ `a53d53f`（db 修复）已推送 master → Render 自动重建。
- 推送前发现**本地 .git 对象严重损坏**（之前手工改 packed-refs/松散 ref 遗留）：`git fetch` 报 `bad object / bad parents / inflate: data stream error`，packed-refs 里还有 `refs/heads/master?` 坏行（带问号）。
- **解法（可复用）**：源码文件无损 → 干净重克隆到 `feiyue-credit-fresh` → 覆盖 src → `git config core.autocrlf false` → 只 add 真实改动文件 → `git checkout -- .` 清掉 66 个 CRLF/LF 噪声文件 → 提交推送 → 最后把新 `.git` 复制回原目录（旧的重命名为 `.git.corrupt.bak/` 并加 .gitignore）→ `git fetch --unshallow` 补全历史。现仓库 99 条历史、tracking 正常。

### 上线后发现的致命 bug（已修）
1. **学生名单永远导不进去**：`student.gender` 列在 `migrate()` 靠后的 6b 步才 ALTER，但 `importClassRoster()` 在最前面（第 522 行）就调用并 INSERT gender → 首次运行 `Unknown column 'gender'` 中断整个 migrate。
   → 修复：gender 列补齐提到 `migrate()` 第 0-pre 步（早于 clearAndRebuild/importClassRoster）。
2. **每次重启都清库（灾难级）**：`seed()` 里 `INSERT INTO badge` 无 IGNORE，而 `clearAndRebuild()` 不清 badge 表 → 徽章 code 唯一键冲突 → seed 抛错 → `clear_rebuild_2026_09_05` 标记写不进去 → 每次重启重复 `DELETE FROM sys_user`。
   → 修复：徽章改 `INSERT IGNORE`；clearAndRebuild 用 try/catch 吞掉 seed 异常并**保证写入迁移标记**；importClassRoster 也包 try/catch（失败不阻断 migrate，下次重启自动重试）。

### 生产核验（2026-09-07 00:46）
- `/api/health` 200；迁移标记三条齐全：`clear_rebuild_2026_09_05` / `import_class_roster_2026_09_05` / `k12_to_neutral_v1`
- sys_user 56 = 2 ADMIN（斐越科技、杨进杰老师）+ 54 STUDENT；student 54 人（男 30 / 女 24）
- 登录实测：杨进杰老师/17344694075 ✓、斐越科技/18603792929 ✓、杨易轩/810810 ✓（mustChangePwd=true）、雷鑫怡/810810 ✓
- 前端资源已上新：CSS 含 `backdrop-filter`（dialog.css 重影修复），JS 含「课代表工作台」（RepPortal）

### 待办
- 图1 第 5 行被高亮遮挡的名字，暂用「李茗」占位，需杨老师核对修正
- 库内暂无 REP（课代表）账号，需管理员在人员管理里把学生设为课代表

// =============================================================================
// [已废弃] 本文件原本是前端 Mock 数据层（沙箱无 Java 后端时的占位）。
// 历史：项目早期 Spring Boot 后端未对接时，通过 VITE_USE_MOCK=true 启用，
//       所有 API 调用返回本地模拟数据，使页面可立即演示。
// 现已对接真实 Express 后端（server/src/），所有页面调用真实 API，mockApi 不再需要。
// 字典常量（statusLabel / flowTypeLabel / typeLabel / calcCredit）已迁至 @/utils/credit。
// 仅保留本文件作为历史备份，可随时安全删除。
// =============================================================================
export {}; // 标记为 ESM 空导出，避免被业务代码意外引用
# 斐越十班学分系统 — 安全事项（迁移包内含）

> 本文件由 WorkBuddy 于 2026-07-21 执行迁移时汇总生成，内容来自迁移包 `feiyue-credit-FULL-migration/README.txt` 的「默认账号」一节。迁移包未做任何安全/体积删减，相关明文信息随包一并保留。

## ⚠️ 默认账号（上线/使用前必须全部修改）

| 角色 | 账号 | 默认密码 | 备注 |
|------|------|----------|------|
| 管理员 | `admin` | `123456` | 弱密码 |
| 教师 | `teacher01` | `123456` | 弱密码 |
| 招生/代表 | `rep01`、`rep02` | `123456` | 弱密码 |
| 学生 | `stu01` ~ `stu06` | `123456` | 弱密码 |
| **超级管理员** | `superadmin` | `Feiyue@2026` | **最高权限，务必最先修改** |

## 重要提醒

1. 以上均为弱密码，且明文存在于迁移包 `README.txt` 与本文件中。请勿将本文件或 README 提交到公开仓库。
2. **一旦系统对外可访问（如已上线的 `https://feiyue-credit.onrender.com`，或本地部署后暴露到公网），请立即修改全部默认密码，尤其是 `superadmin`。**
3. 本地运行方式：进入 `project\` 后双击 `start.bat`（需先安装 Node 22）；业务数据位于 `project\server\data\`。
4. 连接器凭证文件（`~/.workbuddy/connectors/.../.credentials.json`）已随本次迁移合并保留，请勿泄露或提交到版本库。
5. 若此系统用于真实教学/学分场景，建议：为 `superadmin` 启用强口令 + 独立邮箱；为教师/学生账号启用首次登录强制改密。

## 迁移产物位置

- 对话记录主库：`~/.workbuddy/workbuddy.db`（已并入 1 条历史会话 `f51cff76...`）
- 项目对话原文：`~/.workbuddy/projects/c-Users-40814-WorkBuddy-2026-07-19-17-04-34/`
- 项目源码（本目录）：`C:\Users\20242\WorkBuddy\feiyue-credit\`
- 凭证备份：`~/.workbuddy/connectors/`
